source.. = src/
output.. = bin/
bin.includes = META-INF/,\
               .,\
               plugin.xml,\
               icons/,\
               webview/
# The webview/ folder holds the shared web renderer bundle: index.html is
# checked in here (like vs/webview/index.html); main.js / main.css / schematics
# are staged by `npm run build:eclipse-assets` before packaging.
